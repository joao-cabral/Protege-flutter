import 'package:flutter/material.dart';

class OutirgaView extends StatelessWidget {
  const OutirgaView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}
