import 'package:flutter/material.dart';

class HabitaSeView extends StatelessWidget {
  // const HabitaSeView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}
